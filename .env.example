# Development server configuration (useful for Codespaces/containers)
HOST=0.0.0.0
PORT=3000

# File watching (for containers/remote development)
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# Disable auto-opening browser (useful in container environments)
BROWSER=none

# WebSocket port for dev server (uncomment for Codespaces if needed)
# WDS_SOCKET_PORT=443

# API Base URL (placeholder - replace with your actual API endpoint)
# REACT_APP_API_BASE_URL=https://api.example.com

# Note: CRA only exposes environment variables that start with REACT_APP_
# For more info: https://create-react-app.dev/docs/adding-custom-environment-variables/